# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The shared
contract package, the standalone tutorial pipeline module (`src/lmpipeline/pipeline.py`), the offline unit
suite, the snapshot model card (`weights/smollm2-360m/MODEL_CARD.md`) and the two standalone tutorials
`tutorials/language_model_finetuning_colab.ipynb` (`E2E`) and `tutorials/language_model_artifact_inference_colab.ipynb`
(`ARTIFACT-INFERENCE`), generated by `tools/build_notebook.py` under DIMER Notebook Specification 1.1 §3.6,
exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks).
The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is
recorded in `docs/release-verification.md`. The DIMER worker contract status (acceptance runs, platform
blockers) is tracked in `README.md` § Status.
